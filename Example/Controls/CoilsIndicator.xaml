<UserControl x:Class="Example.Controls.CoilsIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Example.Controls"
             xmlns:models="clr-namespace:Example.Models"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border x:Name="IndicatorBorder"
            Background="#303030"
            
            BorderBrush="White"
            BorderThickness="2"
            CornerRadius="10"
            Padding="2"
            Margin="5">
        <Border.Style>
            <Style TargetType="Border">

                <Setter Property="Background" Value="#303030"/>
                <Style.Triggers>
                    
                    <!--COIL ENABLED-->
                    <DataTrigger
                        Binding="{Binding Status,RelativeSource={RelativeSource AncestorType=UserControl}}"
                        Value="{x:Static models:CoilStatus.Enabled}">
                        <Setter Property="Background" Value="#2ECC71"/>
                    </DataTrigger>

                    <DataTrigger
                        Binding="{Binding Status,RelativeSource={RelativeSource AncestorType=UserControl} }"
                        Value="{x:Static models:CoilStatus.Disabled}">
                        <Setter Property="Background" Value="Red"/>
                    </DataTrigger>

                    <DataTrigger
                        Binding="{Binding Status,RelativeSource={RelativeSource AncestorType=UserControl}}"
                        Value="{x:Static models:CoilStatus.Idle}">
                        <Setter Property="Background" Value="LightGray"/>
                    </DataTrigger>

                </Style.Triggers>
            </Style>
            
        </Border.Style>

        <TextBlock Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Foreground="White"
                   
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>


    </Border>
</UserControl>
