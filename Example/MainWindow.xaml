<Window x:Class="Example.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Example" xmlns:viewmodels="clr-namespace:Example.ViewModels"
        
        
        xmlns:controls="clr-namespace:Example.Controls"
        xmlns:models="clr-namespace:Example.Models"
        
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
        
        Title="Example" Height="700" Width="1100" 
        WindowStartupLocation="CenterScreen" 
        ResizeMode="CanResize" WindowState="Normal" MinWidth="1024" MinHeight="640">

    <!-- ========================================= -->
    <!-- LAYOUT PRINCIPAL                         -->
    <!-- ========================================= -->

    <Grid x:Name="MainGrid">

        <Grid.RowDefinitions>
            <!--Main & Sidebar -->
            <RowDefinition Height="*"/>
            <!--Status Bar -->
            <RowDefinition Height="10"/>

        </Grid.RowDefinitions>
        <!-- ========================================= -->
        <!-- MAIN & SIDEBAR                                -->
        <!-- ========================================= -->
        <Grid Grid.Row="0" x:Name="Sidebar_and_Main">
            <Grid.ColumnDefinitions>
                <!--Sidebar -->
                <ColumnDefinition Width="250"/>
                <!--Main -->
                <ColumnDefinition Width="4*"/>
            </Grid.ColumnDefinitions>

            <!-- ========================================= -->
            <!--  SIDEBAR                                 -->
            <!-- ========================================= -->
            <Border Background="{StaticResource Brush.Header}"
                    Padding="15">
                <Grid Grid.Column="0" x:Name="Sidebar">
                
                    <Grid.RowDefinitions>
                        <!--Logo-->
                        <RowDefinition Height=".5*"/>
                        <RowDefinition Height=".7*"/>
                        <!--Buttons-->
                        <RowDefinition Height="1*"/>

                    </Grid.RowDefinitions>
                    
                    <!-- 1.- LOGO -->

                    <Image Grid.Row="0"
                           Source="/Resources/Images/vectorseek.com-Nidec Logo Vector.png"
                           Stretch="Uniform"/>


                    <!-- 2.- PROCESS INFO -->

                    <Grid Grid.Row="1">

                        <Grid.RowDefinitions>   
                            
                            <!--Sequence title-->
                            <RowDefinition Height="*"/>
                            <!--Step 1-->
                            <RowDefinition Height="*"/>
                            <!--Step 2-->
                            <RowDefinition Height="*"/>
                            <!--Step 3-->
                            <RowDefinition Height="*"/>


                        </Grid.RowDefinitions>
                        
                        <!--Sequence title-->

                        <TextBlock
                                Grid.Row="0"
                                   Style="{StaticResource Text.H1}"
                                   Text="Secuencia"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"
                                   Margin="20,0,0,0"
                                   FontSize="34"/>


                        <controls:ProcessIndicator 
                            Grid.Row="1"
                            
                            Text="1"
                            FontSize="24"
                            Background="#303030"
                            Status="{Binding ConnectionVM.PlcStatus}"
                            Width="180"
                            Height="auto"
                            Padding="2"/>

                        <controls:ProcessIndicator
                            Grid.Row="2"
                            Text="2"
                            FontSize="24"
                            Status="{Binding SecondStep}"
                            Width="180"
                            Height="auto"
                            Padding="2"/>

                        <controls:ProcessIndicator
                            Grid.Row="3"
                            Text="3"
                            FontSize="24"
                            Status="{Binding ThirdStep}"
                            Width="180"
                            Height="auto"
                            Padding="2"/>




                    </Grid>
                    
                    


                    <!-- 3.- BUTTONS-->
                    <Grid Grid.Row="2">

                        <Grid.RowDefinitions>
                            <!-- BUTTON 1-->
                            <RowDefinition Height="*"/>
                            <!-- BUTTON 2-->
                            <RowDefinition Height="*"/>
                            <!-- BUTTON 3-->
                            <RowDefinition Height="*"/>
                            <!-- BUTTON 3-->
                            <RowDefinition Height="*"/>

                        </Grid.RowDefinitions>

                        <Button Grid.Row="0" 
                                Style="{StaticResource SidebarMenuStyleButton}"
                                Command="{Binding ShowFirstScreenCommand}">

                            <StackPanel Orientation="Horizontal">

                                <Image Source="/Resources/Images/microchip.png"
                                       Width="32"
                                       Height="32"
                                       Margin="10,0,10,0"/>
                                
                                <TextBlock Text="Gateway"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center" FontSize="28"/>


                            </StackPanel>

                        </Button>

                        <Button Grid.Row="1"
                                Style="{StaticResource SidebarMenuStyleButton}"
                                Command="{Binding ShowconfigurationScreenCommand}" Cursor="Hand" >

                            <StackPanel Orientation="Horizontal">

                                <Image Source="/Resources/Images/cogwheel.png"
                                       Width="32"
                                       Height="32"
                                       Margin="10,0,10,0"/>
                                <TextBlock Text="Configuración"
                                           VerticalAlignment="Center"/>

                            </StackPanel>

                        </Button>

                        <Button Grid.Row="2"
                            Style="{StaticResource SidebarMenuStyleButton}"
                            Command="{Binding ShowSecondScreenCommand}">

                            <StackPanel Orientation="Horizontal">

                                <Image Source="/Resources/Images/folder.png"
                                    Width="32"
                                    Height="32"
                                    Margin="10,0,10,0"/>

                                <TextBlock Text="Trazabilidad"
                                       VerticalAlignment="Center"/>


                            </StackPanel>


                        </Button>

                        <Button Grid.Row="3"
                                Style="{StaticResource SidebarMenuStyleButton}"
                                Command="{Binding ShowSecondScreenCommand}">

                            <StackPanel Orientation="Horizontal">

                                <Image Source="/Resources/Images/logout.png"
                                       Width="32"
                                       Height="32"
                                       Margin="10,0,10,0"/>

                                <TextBlock Text="Cerrar sesión"
                                           VerticalAlignment="Center"/>


                            </StackPanel>
                            
                            
                        </Button>
                                


                    </Grid>
                    
                    


                </Grid>
            </Border>

            <!-- ========================================= -->
            <!--  MAIN AND HEADER                          -->
            <!-- ========================================= -->
            <Grid Grid.Column="1" x:Name="Main_and_Header">
                <Grid.RowDefinitions>
                    <!--Header -->
                    <RowDefinition Height="100"/>
                    <!--Main -->
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <!-- ========================================= -->
                <!--            HEADER                         -->
                <!-- ========================================= -->

                <Grid Grid.Row="0">

                    <Border Background="{StaticResource Brush.Header}">
                        <Grid >

                            <Grid.ColumnDefinitions>
                                <!--Title -->
                                <ColumnDefinition Width="*"/>
                                <!--Indicators-->
                                <ColumnDefinition Width="*"/>
                                <!--Window functions -->
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>

                            <!--Title -->

                            <Grid Grid.Column="0">
                                
                            </Grid>
                            
                            <!--Indicators -->

                            <Grid Grid.Column="1">

                            </Grid>
                            <!--Window functions -->
                            <Grid Grid.Column="2">
                                <Grid.RowDefinitions>
                                    <!--Buttons -->
                                    <RowDefinition Height="*"/>
                                    <!--Unused space -->
                                    <RowDefinition Height="*"/>

                                </Grid.RowDefinitions>
                                <!--Buttons -->
                                <Grid Grid.Row="0">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <!--Minimize -->
                                            <ColumnDefinition Width="*"/>
                                            
                                            <!--Maximize -->

                                            <ColumnDefinition Width="*"/>

                                            <!--Close -->

                                            <ColumnDefinition Width="*"/>

                                        </Grid.ColumnDefinitions>

                                        <!--Minimize -->
                                        <Button Grid.Column="0" 
                                                Width="40"
                                                Height="40"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Padding="0"
                                                Cursor="Hand"
                                                Margin="5"
                                                ToolTip="Minimizar"
                                                Command="{Binding MinimizeCommand}">

                                            <Image Source="/Resources/Images/minimize.png"
                                                   Stretch="Uniform">
                                                
                                            </Image>
                                           
                                            
                                        </Button>
                                        <!--Maximize -->
                                        <Button Grid.Column="1"
                                                Width="40"
                                                Height="40"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Padding="0"
                                                Cursor="Hand"

                                                Margin="5"
                                                ToolTip="Maximizar pantalla"
                                                Command="{Binding MaximizeCommand}">

                                            <Image Source="/Resources/Images/maximize.png"
                                                   Stretch="Uniform"/>

                                        </Button>
                                        <!--Close -->
                                        <Button Grid.Column="2"
                                                Width="40"
                                                Height="40"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Padding="0"
                                                Cursor="Hand"
                                                Margin="5"
                                                ToolTip="Cerrar aplicación"
                                                Command="{Binding CloseCommand}">

                                            <Image Source="/Resources/Images/cerrar2.png"
                                                   Stretch="Uniform"/>


                                        </Button>

                                    </Grid>
                                    
                                </Grid>
                                
                            </Grid>
                        </Grid>
                        
                    </Border>
                    
                </Grid>

                <!-- ========================================= -->
                <!--            MAIN SECTION                   -->
                <!-- ========================================= -->

                <Grid Grid.Row="1">

                    <ContentControl Content="{Binding CurrentView}"/>


                </Grid>

            </Grid>

        </Grid>
        
        <!-- ========================================= -->
        <!-- STATUS BAR                               -->
        <!-- ========================================= -->

        <Grid Grid.Row="1" x:Name="StatusBar" >

            <Border Background="{Binding AppState.Status,
                             Converter={StaticResource StatusBrushConverter}}"/>
                
         
            
        </Grid>

    </Grid>
   
</Window>
